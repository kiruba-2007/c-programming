#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int a=1234,sum=0;
    while(a>0)
        {
        int rem=a%10;
        sum=sum+rem;
        a=a/10;
        }
    printf("Sum of digit is %d",sum);
    return 0;
    }
